# Cross-origin plugins execute inside the app and can access browser-local data.
# Keep disabled unless the plugin source has been reviewed and explicitly trusted.
VITE_ENABLE_REMOTE_PLUGINS=false

# Keep the standalone canvas independent from Canvas Agent by default.
VITE_ENABLE_AGENT_AUTOCONNECT=false
